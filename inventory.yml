all:
  children:
    vmware:
      hosts:
      vars:
        vcenter_hostname: "vcsa04.ati.pi.gov.br"
        datacenter_name: "ATI-SLC-HCI"
        validate_certs: false
    powered_on:
      hosts:
    windows:
      hosts:
    linux:
      hosts:
