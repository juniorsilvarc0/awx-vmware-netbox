all:
  children:
    vmware_vms:
      hosts:
        # Template para adicionar VMs manualmente
        # vm-example:
        #   ansible_host: 192.168.1.100
        #   vm_name: "vm-example"
        #   vm_power_state: "poweredOn"
        #   vm_guest_os: "Windows Server 2019"
        #   vm_cpu_count: 4
        #   vm_memory_mb: 8192
        #   vm_datacenter: "ATI-SLC-HCI"
        #   vm_cluster: "Cluster01"
      vars:
        # Variáveis globais para todas as VMs
        vcenter_hostname: "vcsa04.ati.pi.gov.br"
        datacenter_name: "ATI-SLC-HCI"
        validate_certs: false
        inventory_type: "static_vmware"

    # Grupos por estado (inicialmente vazios)
    powered_on:
      hosts:

    powered_off:
      hosts:

    windows:
      hosts:

    linux:
      hosts:
